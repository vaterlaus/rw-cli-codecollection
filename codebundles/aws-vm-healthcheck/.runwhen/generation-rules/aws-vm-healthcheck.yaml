apiVersion: runwhen.com/v1
kind: GenerationRules
spec:
  platform: aws
  generationRules:
    - resourceTypes:
        - ec2_instance
      matchRules:
        - type: pattern
          pattern: ".+"
          properties: [name]
          mode: substring
      slxs:
        - baseName: aws-vm-health
          levelOfDetail: basic
          # qualifiers: [project]
          baseTemplateName: aws-vm-health
          outputItems:
            - type: slx
            - type: runbook
              templateName: aws-vm-health-taskset.yaml
    - resourceTypes:
        - aws_ec2_instances
      matchRules:
        - type: pattern
          pattern: ".+"
          properties: [name]
          mode: substring
      slxs:
        - baseName: aws-vm-health2
          levelOfDetail: basic
          # qualifiers: [project]
          baseTemplateName: aws-vm-health
          outputItems:
            - type: slx
            - type: runbook
              templateName: aws-vm-health-taskset.yaml
